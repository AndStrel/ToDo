const path = require('path')
module.exports = {
	mode: 'production',
	devtool: false,
	// output: {
	// 	path: path.resolve(__dirname, './dist'),
	// 	filename: 'bundle.js',
	// 	publicPath: production ? '/ToDo/' : '/', // <-- замените 'ToDo' на название вашего репозитория
	// },
}
